{
  "name": "agent-passport",
  "private": true,
  "version": "0.0.0",
  "description": "The connection passport for AI apps.",
  "homepage": "https://passport.orchestrator.so",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/UseOrchestrator/agent-passport.git"
  },
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "yarn workspace @agent-passport/sdk build",
    "typecheck": "yarn workspace @agent-passport/sdk typecheck && yarn workspace @agent-passport/composio-agent-mission typecheck",
    "example:composio": "yarn workspace @agent-passport/composio-agent-mission dev"
  },
  "license": "MIT"
}
